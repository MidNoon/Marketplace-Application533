<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.marketplaceaplo5">

    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MarketplaceAplo5">
        <activity android:name=".UBoughtItemsActivity"></activity>
        <activity android:name=".USoldItemsActivity" />
        <activity android:name=".UViewItemsActivity" />
        <activity android:name=".UAddItemActivity" />
        <activity android:name=".ItemPurchaseActivity" />
        <activity android:name=".ItemManagementActivity" />
        <activity android:name=".InvoiceActivity" />
        <activity android:name=".SettingsActivity" />
        <activity android:name=".BrowseActivity" />
        <activity
            android:name=".RegistrationActivity"
            android:parentActivityName=".MainActivity" />
        <activity android:name=".LoginActivity" />
        <activity
            android:name=".ResetPasswordActivity"
            android:parentActivityName=".MainActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>